# ============================================================
# LancasterLink Backend – Python Dependencies
# ============================================================

# --- Web Framework & Server ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# --- Database ---
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0       # PostgreSQL adapter
alembic>=1.13,<2.0              # Database migrations
geoalchemy2>=0.15,<1.0          # PostGIS integration for SQLAlchemy

# --- Caching ---
redis>=5.0,<6.0

# --- HTTP Client (external API polling) ---
httpx>=0.27,<1.0

# --- STOMP Client (rail live data via Network Rail) ---
stomp.py>=8.1,<9.0

# --- Data Processing ---
pandas>=2.2,<3.0
shapely>=2.0,<3.0               # Geometric operations

# --- Scheduling (data-ingestion worker) ---
apscheduler>=3.10,<4.0

# --- Validation & Settings ---
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# --- Testing ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
httpx                            # Also used as AsyncClient in tests
