# ============================================================
# LancasterLink – Environment Variables Template
#
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# docker compose reads .env automatically from the project root.
# ============================================================

# ----- PostgreSQL Database -----
POSTGRES_USER=lancasterlink
POSTGRES_PASSWORD=changeme_in_production
POSTGRES_DB=lancasterlink
DB_PORT=5432

# ----- Redis Cache -----
REDIS_PORT=6379

# ----- Backend API -----
FRONTEND_PORT=8080
BACKEND_WORKERS=4
LOG_LEVEL=info

# ----- External Data APIs -----
# Bus Open Data Service (BODS) API key
# Register at: https://data.bus-data.dft.gov.uk/
BODS_API_KEY=

# Network Rail STOMP feed credentials
# Register at: https://datafeeds.networkrail.co.uk/
NETWORK_RAIL_USER=
NETWORK_RAIL_PASS=

# ----- Data Ingestion Settings -----
# How often to poll live APIs (seconds) — must respect rate limits (DR-03)
POLLING_INTERVAL_SECONDS=30

# Time-to-live for cached vehicle data (seconds) — NFR-RA-02: 5 min max
STALE_DATA_TTL_SECONDS=300
